version: "3"

networks:
  web:
    external: true

services:
  traefik:
    image: traefik:v2.2
    restart: unless-stopped
    container_name: traefik
    networks:
      - web
    ports:
      - "80:80"
      - "443:443"
    labels:
      - "traefik.http.middlewares.bauth.basicauth.users=drew:$$apr1$$XOOG.BJt$$hCm3v3MiAzqXJLn9oQvkU1,farza:$$apr1$$YzdxnXKZ$$T3NipjpUZKJI6oBMmrL041"
    volumes:
      - "./traefik.toml:/traefik.toml"
      - "./acme.json:/acme.json"
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
